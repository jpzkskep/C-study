//도전7(틀림)
//2의 k승이 n과 작거나 같다.
//사용자로부터 n을 입력받고 k의 최대값을 구하는 프로그램
#include <stdio.h>
void whichmax(int num);
int main(void) {
	int n;
	printf("상수 n 입력:");
	scanf("%d", &n);
	whichmax(n);
}
void whichmax(int num) {
	int k = 0;
	for (int i = 2;; i * i) {

	}
}